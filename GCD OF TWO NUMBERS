#include <stdio.h>

int main()
{
    int a = 24, b = 18, gcd, i;

    for (i = 1; i <= a && i <= b; i++)
    {
        if (a % i == 0 && b % i == 0)
        {
            gcd = i;
        }
    }

    printf("GCD = %d\n", gcd);

    return 0;
}
